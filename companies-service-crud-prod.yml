spring:
  datasource:
    url: jdbc:postgresql://db-companies:5432/companies
    username: alan-tec
    password: iledev35
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
